import SwiftUI

// The main entry point for the TravelApp application
@main
struct TravelAppApp: App {
    var body: some Scene {
        // The main window group for the app, displaying ContentView as the root view
        WindowGroup {
            ContentView()
        }
    }
}
